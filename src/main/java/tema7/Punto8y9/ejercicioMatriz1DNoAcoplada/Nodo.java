package tema7.Punto8y9.ejercicioMatriz1DNoAcoplada;

// No he puesto public delante por lo que la clase es accesible sólo
// desde las clases hermanas (están dentro del mismo paquete)
class Nodo {
    // No pongo private a los atributos y si modificador por defecto
    // para que puedan ser accesibles por las clases hermanas
    int indice;
    int valor;
    Nodo siguiente;
}
